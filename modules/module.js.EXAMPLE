const Discord = require("discord.js");
const db = require("quick.db");

const config = require("../config.json");

module.exports = {
	name: "module",
	startup: true,
	execute(client) {
		// Module code
	},
};
